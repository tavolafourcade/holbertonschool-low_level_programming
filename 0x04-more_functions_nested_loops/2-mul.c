#include "holberton.h"
/**
 * mul - It multiplies two integers
 * @a: first value to be evaluated
 * @b: second value to be evaluated
 * Return: (1) if c is uppercase (0) otherwise
 */
int mul(int a, int b)
{
return (a * b);
}
