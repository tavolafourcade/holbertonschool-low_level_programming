#include <stdio.h>
/**
 * main - program that prints its name.
 * @argc: argument count.
 * @argv: argument vector.
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
